# ML Models Directory

Place your trained machine learning models here:

- `gesture_model.pkl` - Gesture recognition model (scikit-learn)
- `sign_detector.h5` - Sign detection model (TensorFlow/Keras)

The system will work with mock predictions if these files are not present.
